let commissionerTab='overview';const COMM_KEY='leaguehq_commissioner_v1';function commDefaults(){return{commissionerNote:'',rulesUrl:'',sleeperUrl:'https://sleeper.com/leagues/1321261643883094016',leagueSafeUrl:'',tradeDeadlineNote:'',constitution:'',links:[]}}function commLoad(){try{return{...commDefaults(),...JSON.parse(localStorage.getItem(COMM_KEY)||'{}')}}catch{return commDefaults()}}function commSave(patch){localStorage.setItem(COMM_KEY,JSON.stringify({...commLoad(),...patch}));render()}function setCommissionerTab(id){commissionerTab=id;render();window.scrollTo({top:0,behavior:'smooth'})}window.setCommissionerTab=setCommissionerTab;
function saveCommField(id,key){const el=document.getElementById(id);if(el)commSave({[key]:el.value})}window.saveCommField=saveCommField;window.setCommissionerTab=setCommissionerTab;
function commOverview(){const c=commLoad();return `<div class="section-panel"><div class="panel-heading"><div><div class="eyebrow">LEAGUE ADMIN</div><h2>Commissioner Dashboard</h2></div></div><div class="grid grid-3">${card('League',S.league.name||'League HQ',`${S.rosters.length} teams`)}${card('Scoring',currentFormat(),`${S.league.settings?.playoff_teams||6} playoff teams`)}${card('Season',S.league.season||'2026',`Week ${S.league.settings?.leg||1}`)}</div><div class="card commissioner-note"><h3>Commissioner Note</h3><textarea id="commNote" placeholder="Post a league-wide note, deadline reminder, rule clarification, etc.">${esc(c.commissionerNote)}</textarea><button onclick="saveCommField('commNote','commissionerNote')">Save Note</button></div>${c.commissionerNote?`<div class="card announcement"><div class="eyebrow">COMMISSIONER ANNOUNCEMENT</div><p>${esc(c.commissionerNote)}</p></div>`:''}</div>`}
function rulesPanel(){const c=commLoad(),positions=(S.league.roster_positions||[]).join(', ');return `<div class="section-panel"><div class="panel-heading"><div><div class="eyebrow">LEAGUE CONFIGURATION</div><h2>Rules & Settings</h2></div></div><div class="card table-wrap"><table class="table"><tbody><tr><th>Teams</th><td>${S.rosters.length}</td></tr><tr><th>Roster</th><td>${esc(positions)}</td></tr><tr><th>Playoff teams</th><td>${S.league.settings?.playoff_teams||'—'}</td></tr><tr><th>Draft rounds</th><td>${S.league.settings?.draft_rounds||'—'}</td></tr><tr><th>Waiver budget</th><td>${S.league.settings?.waiver_budget||'Sleeper setting'}</td></tr></tbody></table></div><div class="card commissioner-note"><h3>League Constitution / Rule Notes</h3><textarea id="constitution" placeholder="Paste league rules or commissioner clarifications here.">${esc(c.constitution)}</textarea><button onclick="saveCommField('constitution','constitution')">Save Rules</button></div></div>`}
function linksPanel(){const c=commLoad();return `<div class="section-panel"><div class="panel-heading"><div><div class="eyebrow">LEAGUE LINKS</div><h2>Commissioner Links</h2></div></div><div class="grid grid-2"><div class="card comm-link-editor"><label>Sleeper League URL<input id="sleeperLink" value="${esc(c.sleeperUrl)}"></label><button onclick="saveCommField('sleeperLink','sleeperUrl')">Save</button>${c.sleeperUrl?`<a href="${esc(c.sleeperUrl)}" target="_blank" rel="noopener">Open Sleeper ↗</a>`:''}</div><div class="card comm-link-editor"><label>LeagueSafe / Payment URL<input id="leagueSafeLink" value="${esc(c.leagueSafeUrl)}" placeholder="Optional"></label><button onclick="saveCommField('leagueSafeLink','leagueSafeUrl')">Save</button>${c.leagueSafeUrl?`<a href="${esc(c.leagueSafeUrl)}" target="_blank" rel="noopener">Open Payment Site ↗</a>`:''}</div><div class="card comm-link-editor"><label>Rules / Constitution URL<input id="rulesLink" value="${esc(c.rulesUrl)}" placeholder="Google Doc, Notion, etc."></label><button onclick="saveCommField('rulesLink','rulesUrl')">Save</button>${c.rulesUrl?`<a href="${esc(c.rulesUrl)}" target="_blank" rel="noopener">Open Rules ↗</a>`:''}</div><div class="card comm-link-editor"><label>Trade Deadline / Key Date Note<input id="deadlineNote" value="${esc(c.tradeDeadlineNote)}" placeholder="Example: Trade deadline Week 11"></label><button onclick="saveCommField('deadlineNote','tradeDeadlineNote')">Save</button></div></div><p class="fineprint">Commissioner customizations are stored in this browser for V1. Shared cloud persistence can be added later if the league needs multiple admins editing the same settings.</p></div>`}
function settingsPanel(){return `<div class="section-panel"><div class="panel-heading"><div><div class="eyebrow">DATA SOURCES</div><h2>League HQ Configuration</h2></div></div><div class="card table-wrap"><table class="table"><tbody><tr><th>Sleeper League ID</th><td><code>${LEAGUE}</code></td></tr><tr><th>Dynasty format</th><td>${currentFormat()}</td></tr><tr><th>Dynasty values</th><td>RosterAudit market layer</td></tr><tr><th>Sportsbook</th><td>SportsGameOdds server feed</td></tr><tr><th>Weekly report</th><td>Website-only · Tuesday 9:00 AM CT rollover</td></tr></tbody></table></div><div class="card methodology"><h3>Admin boundary</h3><p class="muted">League HQ reads Sleeper data but does not change Sleeper lineups, process wagers, or hold league funds. Those actions remain with their respective services.</p></div></div>`}
function commissionerView(){const tabs=[{id:'overview',label:'Dashboard'},{id:'rules',label:'Rules & Settings'},{id:'links',label:'League Links'},{id:'settings',label:'Site Configuration'}];const p=commissionerTab==='rules'?rulesPanel():commissionerTab==='links'?linksPanel():commissionerTab==='settings'?settingsPanel():commOverview();return `${innerNav(tabs,commissionerTab,'setCommissionerTab')}${p}`}
const commissionerBaseRender=render;render=function(){if(S.view==='commissioner'){$('#pageTitle').textContent='Commissioner Center';root.innerHTML=commissionerView();return}commissionerBaseRender()};window.render=render;