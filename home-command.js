function dashboardMatchup(){const groups={};(S.matchups||[]).forEach(m=>(groups[m.matchup_id]??=[]).push(m));const games=Object.values(groups).filter(g=>g.length===2).map(g=>({g,margin:Math.abs(Number(g[0].points||0)-Number(g[1].points||0))})).sort((a,b)=>a.margin-b.margin);return games[0]?.g||null}
function recentTxSummary(){return(S.tx||[]).slice(0,5)}
function txLabel(t){if(t.type==='trade')return 'Trade completed';if(t.type==='waiver')return 'Waiver processed';if(t.type==='free_agent')return 'Free-agent move';return (t.type||'Transaction').replace(/_/g,' ')}
function txTeams(t){const ids=new Set([...(t.roster_ids||[]),...Object.values(t.adds||{}),...Object.values(t.drops||{})].map(Number));return [...ids].filter(Boolean).map(id=>name(roster(id))).join(' · ')}
function topMoverRows(){return [...(S.movers||[])].map(x=>{const id=String(x.sleeper_id||x.player_id||x.id||'');return{id,name:x.name||pname(id),move:Number(x.trend_7d||x.change||0)||0,value:dynastyValue(id)}}).filter(x=>x.id).sort((a,b)=>Math.abs(b.move)-Math.abs(a.move)).slice(0,5)}
function homeWaivers(){return typeof freeAgents==='function'?freeAgents().slice(0,5):[]}
function homeOutlooks(){return typeof outlookRows==='function'?outlookRows():[]}
function homeQuickLink(view,title,sub,icon){return `<button class="home-quick card" onclick="go('${view}')"><span>${icon}</span><div><strong>${esc(title)}</strong><small>${esc(sub)}</small></div><b>→</b></button>`}
function homeStandings(){return standings().slice(0,5)}
function dashboardView(){const power=powerRankings(),st=homeStandings(),m=dashboardMatchup(),movers=topMoverRows(),waivers=homeWaivers(),outlooks=homeOutlooks(),tx=recentTxSummary(),wk=S.league.settings?.leg||1,leader=st[0],power1=power[0],future=outlooks[0];return `<div class="home-hero"><div><div class="eyebrow">${esc(S.league.season||'2026')} LEAGUE COMMAND CENTER</div><h2>${esc(S.league.name||'League HQ')}</h2><p>Everything happening across the league, distilled into one starting point.</p></div><div class="home-week"><small>CURRENT WEEK</small><strong>${wk}</strong></div></div><div class="grid grid-4 home-kpis">${card('Standings Leader',leader?name(leader):'—',leader?rec(leader):'')}${card('Dynasty Power #1',power1?name(power1.r):'—',power1?fmt(power1.total)+' total value':'')}${card('Best Long-Term Profile',future?name(future.r):'—',future?future.label:'')}${card('League Trades',S.tx.filter(t=>t.type==='trade'&&t.status==='complete').length,'Current season')}</div><div class="home-layout"><div class="home-main"><div class="card home-panel"><div class="home-panel-head"><div><div class="eyebrow">WEEK ${wk}</div><h3>Matchup Spotlight</h3></div><button onclick="go('matchups')">All matchups →</button></div>${m?`<div class="spotlight-game">${m.map(x=>{const r=roster(x.roster_id);return `<button onclick="openTeam(${x.roster_id})"><span>${esc(name(r))}</span><strong>${Number(x.points||0).toFixed(2)}</strong><small>${rec(r)}</small></button>`}).join('<div class="versus">VS</div>')}</div>`:'<p class="muted">Weekly matchup data will appear here when Sleeper posts the slate.</p>'}</div><div class="grid grid-2"><div class="card home-panel"><div class="home-panel-head"><div><div class="eyebrow">TOP FIVE</div><h3>Standings</h3></div><button onclick="go('standings')">Full table →</button></div><div class="home-rank-list">${st.map((r,i)=>`<button onclick="openTeam(${r.roster_id})"><span class="home-rank">${i+1}</span><div><strong>${esc(name(r))}</strong><small>${rec(r)} · ${pts(r)} PF</small></div></button>`).join('')}</div></div><div class="card home-panel"><div class="home-panel-head"><div><div class="eyebrow">DYNASTY MARKET</div><h3>Biggest Movers</h3></div><button onclick="go('values')">Player values →</button></div><div class="home-rank-list">${movers.map(x=>`<button onclick="openPlayer('${x.id}','home')"><span class="${x.move>=0?'home-up':'home-down'}">${x.move>=0?'▲':'▼'}</span><div><strong>${esc(x.name)}</strong><small>${fmt(x.value)} value · ${x.move>=0?'+':''}${fmt(x.move)}</small></div></button>`).join('')||'<span class="muted">No market movement available.</span>'}</div></div></div><div class="grid grid-2"><div class="card home-panel"><div class="home-panel-head"><div><div class="eyebrow">AVAILABLE NOW</div><h3>Waiver Watch</h3></div><button onclick="go('waivers')">Waiver Wire →</button></div><div class="home-rank-list">${waivers.map((x,i)=>`<button onclick="openPlayer('${x.id}','home')"><span class="home-rank">${i+1}</span><div><strong>${esc(x.name)}</strong><small>${esc(x.pos)} · ${esc(x.team||'FA')} · ${fmt(x.value)}</small></div></button>`).join('')||'<span class="muted">No valued free agents found.</span>'}</div></div><div class="card home-panel"><div class="home-panel-head"><div><div class="eyebrow">RECENT MOVES</div><h3>League Activity</h3></div><button onclick="go('activity')">Activity Center →</button></div><div class="home-activity">${tx.map(t=>`<div><span>${esc(txLabel(t))}</span><strong>${esc(txTeams(t)||'League transaction')}</strong><small>${date(t.created)}</small></div>`).join('')||'<span class="muted">No recent transactions.</span>'}</div></div></div></div><aside class="home-side"><div class="card home-panel"><div class="eyebrow">QUICK ACCESS</div><h3>League Tools</h3><div class="home-quick-grid">${homeQuickLink('trades','Trade Center','Build and value deals','⇄')}${homeQuickLink('finder','Trade Matches','Find natural partners','◇')}${homeQuickLink('draft','Draft Center','Capital, grades, prospects','★')}${homeQuickLink('sportsbook','Sportsbook','NFL props and lines','＋')}${homeQuickLink('schedule','Schedule Center','Strength of schedule','◫')}${homeQuickLink('reports','Weekly Report','Tuesday league edition','▣')}</div></div><div class="card home-panel"><div class="home-panel-head"><div><div class="eyebrow">FRANCHISE WINDOWS</div><h3>League Snapshot</h3></div><button onclick="go('outlook')">Outlook →</button></div><div class="home-window-list">${outlooks.slice(0,5).map(x=>`<button onclick="openTeam(${x.r.roster_id})"><strong>${esc(name(x.r))}</strong><span>${esc(x.label)}</span></button>`).join('')}</div></div><div class="card home-panel home-report"><div class="eyebrow">TUESDAY 9 AM CT</div><h3>Weekly League Report</h3><p class="muted">Standings, power movement, transactions, dynasty stock, prospect notes and matchup storylines refresh in the website report every Tuesday.</p><button onclick="go('reports')">Read latest report →</button></div></aside></div>${attribution()}`}
const homeCommandBaseRender=render;render=function(){if(S.view==='home'){$('#pageTitle').textContent='League Headquarters';root.innerHTML=dashboardView();return}homeCommandBaseRender()};window.render=render;
